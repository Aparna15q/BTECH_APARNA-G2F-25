#include <stdio.h>

int sumOfDigits(int n) {
    // Base case
    if (n == 0) {
        return 0;
    }
    // Recursive case
    return (n % 10) + sumOfDigits(n / 10);
}

int main() {
    int num = 12345;
    printf("Sum of digits of %d is: %d\n", num, sumOfDigits(num));
    return 0;
}